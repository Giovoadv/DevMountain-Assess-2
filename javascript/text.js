function getZ() {
    const nestedLetters = ['m', 'g', 'e', 'q', 'h', ['n', 'b', ['v', 'z', 'y', 'r']], 'a'];
    for(let i = 0; i < nestedLetters.length; i++){
      if(nestedLetters[i] === 'z'){
  
      }
    }
    console.log(nestedLetters);
}
